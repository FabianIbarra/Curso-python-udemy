import os
os.system("cls" if os.name == "nt" else "clear")

my_set = {7, 8, 9, 1, 2, 3, 4, 5}
print(my_set)

# Colección desordenada
# No tiene indices
# No tiene duplicados
# Óptimo para operaciones matemáticas

dnis = {123, 456, 789, 123, 456, 789, 123, 456, 789}
print(dnis)

usernames = {"gabi02", "temu8", "fabi_iba4", "gabi02", "temu8", "fabi_iba4"}
print(usernames)